# デバッグ・チャレンジ

## 講師用

### 授業のねらい

シューティングゲームを動かし、実際の動きとコードを見比べながら、少しずつバグを直す。最終的には、次の考え方を体験する。

- VS Codeでファイルを開き、保存して実行する
- プログラムが上から順に動き、関数が呼び出されることを知る
- `if` で条件に応じた処理を行う
- `for` で敵や弾を1つずつ調べる
- コードを少し変え、ゲームの動きを試す

対象は小学3年生〜中学1年生、授業時間はプレイ時間を含めて約90分とする。

### 使用するファイル

- 生徒が編集するファイル：`mondai.py`
- 完成例・講師用の答え：`mondai_kotae.py`
- 実行に必要な画像：`images/player.png`、`images/enemy.png`、`images/bullet.png`

`mondai.py` と `mondai_kotae.py` は同じゲームの状態を表す。生徒用ファイルには、基礎3課題と、最後に取り組む発展課題が入っている。

### 事前準備

1. 各PCでリポジトリを開く。
2. `mondai.py` と `images/` が同じフォルダにあることを確認する。
3. ターミナルで次を実行できることを確認する。

```sh
uv run python mondai.py
```

4. VS Codeで保存する方法、ターミナルで実行する方法、Escでゲームを終了する方法を確認する。

### 90分の進行例

| 時間 | 内容 |
|---:|---|
| 0〜10分 | 表紙、名前、VS Codeと実行方法の確認 |
| 10〜20分 | ゲームを遊び、変な動きを見つける |
| 20〜30分 | 問題1：じぶんの左右移動 |
| 30〜50分 | 問題2：弾と敵の衝突、`for` |
| 50〜65分 | 問題3：敵との衝突、ゲームオーバー |
| 65〜75分 | 3問を完成させて動作確認 |
| 75〜87分 | 発展チャレンジ |
| 87〜90分 | 共有と振り返り |

### 操作方法

| キー | 動き |
|---|---|
| ← → | じぶんを左右に動かす |
| Space | たまを出す |
| I | 四角と絵を切り替える |
| Enter | ゲームオーバー後に最初からやり直す |
| Esc | ゲームを終わる |

### 課題1：じぶんを動かす

#### 作るものと作業ファイル

- 作るもの：左右キーで動く赤い四角
- 作業ファイル：`mondai.py` の `じぶんの動き` 関数

#### 残すもの・変えるもの

- 残すもの：キーを調べる `if`、画面の端から出さない処理、関数の呼び出し
- 変えるもの：左右キーを押したときに、じぶんを動かす命令を追加する

#### 支援の順番

1. `じぶんの動き(おされているキー)` の定義を探す。
2. メインループの `じぶんの動き(...)` を探し、関数が呼ばれていることを確認する。
3. 左右のキーの中にある `pass` を見つける。
4. 分からない場合は、`mondai_kotae.py` の左右移動2行を書き写す。

#### 答え

```python
if おされているキー[pygame.K_LEFT]:
    じぶん.move_ip(-いどうそくど, 0)
elif おされているキー[pygame.K_RIGHT]:
    じぶん.move_ip(いどうそくど, 0)
```

#### 完成確認

左右キーで動き、画面の外へはみ出さないことを確認する。

### 課題2：たまと敵を調べる

#### 作るものと作業ファイル

- 作るもの：たまが敵に当たると、敵が消えて得点が増えるゲーム
- 作業ファイル：`mondai.py` の `たまが敵に当たったか` 関数

#### 残すもの・変えるもの

- 残すもの：たまの移動、画面外のたまを消す処理、得点表示
- 変えるもの：敵のリストを `for` で1つずつ調べ、当たったときの処理を書く

#### 支援の順番

1. 敵は `てきたち` というリストに入っていることを確認する。
2. リストの中身を1つずつ調べるには `for` を使うことを確認する。
3. `colliderect` でたまと敵が重なったかを調べる。
4. 分からない場合は答えページのまとまりを書き写す。

#### 答え

```python
for てき in てきたち:
    if たま.colliderect(てき):
        てきたち.remove(てき)
        とくてん += 10
        return True
```

#### 完成確認

敵にたまを当てると敵が消え、SCOREが10増え、たまも消えることを確認する。

### 課題3：メインループでゲームオーバーにする

#### 作るものと作業ファイル

- 作るもの：敵にぶつかるとゲームオーバーになるゲーム
- 作業ファイル：`mondai.py` の敵を動かすメインループ

#### 残すもの・変えるもの

- 残すもの：敵を動かす `for`、敵を画面外で消す処理、ゲームオーバー画面
- 変えるもの：自分と敵が当たったときの `if` と代入を書く

#### 支援の順番

1. 敵を1つずつ動かしている `for` の中を見る。
2. `じぶん.colliderect(てき)` で重なりを調べる。
3. 当たったときは、ゲームオーバーを表す値を `True` にする。
4. この処理はメインループ内なので、`nonlocal` は使わない。

#### 答え

```python
if じぶん.colliderect(てき):
    ゲームオーバー = True
```

#### 完成確認

敵にぶつかるとGAME OVERが表示され、Enterで最初からやり直せることを確認する。

### 発展チャレンジの進め方

まず基礎3課題が動く状態にする。時間があれば、3方向弾を追加する。その後、1か所だけ変更して動きを予想し、実行して確かめる。

#### 3方向弾

`mondai.py` にはまっすぐ上へ出るたまが1つ用意されている。`たまを出す` の3つ目の数を変え、右ななめ上と左ななめ上のたまを追加する。

完成例は `mondai_kotae.py` を参照する。

#### 自分でゲームを変える

変更は1か所ずつ行い、うまく動かないときに戻せるよう、変更前のファイルを保存しておく。

| 変える場所 | 予想する動き |
|---|---|
| `たまのそくど` | たまが速くなる／遅くなる |
| `てきのでにくさ` | 敵が出る間かくが変わる |
| `じぶんのいろ` | じぶんの色が変わる |
| `-たまのそくど` | たまが下へ進む |
| 左右の移動命令 | 左右キーの動きが逆になる |
| `とくてん += 10` | 1回に増える点が変わる |
| `がぞうモード = False` | 最初から絵で表示される |
| 敵を消す処理 | 敵が消えないなど、動きが変わる |

### 振り返り

- 一番見つけるのが難しかった処理はどれか。
- どの動きとコードを見比べてバグを見つけたか。
- 自分で変えて面白かったところはどこか。

## 生徒用スライド原稿

<!-- Marpでこの見出し以降を抽出して使用する。 -->

<!-- STUDENT_SLIDES_START -->

---
marp: true
theme: default
paginate: true
---

# 🐛 バグをさがせ！

## デバッグ・チャレンジ

シューティングゲームを直しながら、Pythonプログラミングを体験しよう。

名前：　　　　　　　　　　年　　　組

---

# 問題1　じぶんが動かない

## やること

1. `mondai.py` を実行する。
2. ← → を押して、赤い四角の動きを見る。
3. `じぶんの動き` の中を調べる。
4. 左右キーを押したときの動きを完成させる。

## ヒント

- キーを調べる `if` の中を見よう。
- `pass` は「なにもしない」という命令。
- 画面の端から出ない処理は残す。

---

# 答え1　じぶんを動かす

左右キーの中に、じぶんを動かす命令を書く。

```python
if おされているキー[pygame.K_LEFT]:
    じぶん.move_ip(-いどうそくど, 0)
elif おされているキー[pygame.K_RIGHT]:
    じぶん.move_ip(いどうそくど, 0)
```

直ったら、画面の端で止まることも確認しよう。

---

# 問題2　たまが敵をすりぬける

## やること

たまを敵に当てても、敵が消えず、SCOREも増えない。

`たまが敵に当たったか` の中に、次の処理を書こう。

- 敵を1つずつ調べる
- たまと敵が重なったか調べる
- 当たった敵を消す
- 得点を10増やす
- 当たったことを知らせる

---

# 答え2　たまと敵を調べる

```python
for てき in てきたち:
    if たま.colliderect(てき):
        てきたち.remove(てき)
        とくてん += 10
        return True
```

`for` はリストの中身を1つずつ調べる繰り返し。

---

# 問題3　敵にぶつかっても平気

## やること

じぶんと敵がぶつかっても、GAME OVERにならない。

敵を動かしているメインループに、次の処理を考えて書こう。

- じぶんと敵が当たったか調べる
- 当たっていたらゲームオーバーにする

ヒント：`colliderect` を使う。

---

# 答え3　ゲームオーバーにする

```python
if じぶん.colliderect(てき):
    ゲームオーバー = True
```

完成したら、敵にぶつかる、GAME OVER、Enterで再開、の順に確認しよう。

---

# はってんチャレンジ

## ① 3方向弾を作る

まっすぐ上、右ななめ上、左ななめ上の3方向にたまを出そう。

## ② 自分でゲームを変える

1か所だけ変えて、どうなるか予想してから実行しよう。

| 変える場所 | どうなるかな？ |
|---|---|
| たまの速さ | たまが速く／遅くなる |
| 敵の出現間かく | 敵が出る間かくが変わる |
| じぶんの色 | じぶんの色が変わる |
| 弾の上下方向 | たまの進む向きが変わる |
| 左右キーの命令 | 左右の動きが逆になる |
| 得点の数字 | 1回に増える点が変わる |
| 画像モード | 最初から絵で表示される |
| 敵を消す処理 | 敵が消えないなどの変化が起きる |

変更前のファイルを保存しておくと安心。
